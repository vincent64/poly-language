¦ The Queue interface describes an abstract FIFO queue structure.
¦ It contains methods to push and pop object, as well as to get the first object.
¦ @note First-in-First-out (FIFO)
¦ @author Vincent Philippe (@vincent64)
¦ @version 0.1
¦
interface+ Queue(Deque) {

    ¦ Pushes the given object to the queue.
    ¦ @param the object
    ¦
    fn void push(Object obj);

    ¦ Pops the first object off the queue and returns it.
    ¦ @returns the topmost object
    ¦
    fn Object pop();

    ¦ Pops the n first objects off the queue and returns them in a list.
    ¦ @param the object count
    ¦ @returns the first objects list
    ¦
    fn List pop(int count) {
        assert(0 <= count && count <= size());

        List list = new ArrayList();
        for(int i = 0; i < count; i++)
            list.add(pop());

        return list;
    }

    ¦ Returns the first object.
    ¦ @returns the first object
    ¦
    fn Object peek();
}